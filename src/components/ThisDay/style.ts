import styled from 'styled-components';

export const DateNow = styled.p`
    margin: 10px 0;
    font-size: 1.5rem;
    color: white;
    font-style: italic;
    font-weight: 600;
`;

export const Title = styled.h2`
    color: white;
    font-size: 2.6rem;
`;

export const ThisDayContainer = styled.div`
    margin-bottom: 20px;
`;
