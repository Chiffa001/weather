import styled from 'styled-components';
import List from '../ui/List';

export const TemperatureList = styled(List)`
    font-size: 1.1rem;
    font-style: italic;
    font-weight: 600;

    color: white;
`;
